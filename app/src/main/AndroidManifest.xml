<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto"> <!-- Harici depolamaya da kurulmaya izin ver -->

    <!-- TEMEL İZİNLER -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- İnternet erişimi - Gemini API için -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- Mikrofon - sesli komutlar için -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> <!-- Bildirim gönderme -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- Telefon açılınca otomatik başlatma -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- Arka planda çalışma -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" /> <!-- Arka planda mikrofon kullanımı -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" /> <!-- Tam ekran bildirimler -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" 
        tools:ignore="ProtectedPermissions"/> <!-- Bildirim okuma -->

    <!-- SİSTEM KONTROLÜ İÇİN EK İZİNLER -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" 
        tools:ignore="ProtectedPermissions"/> <!-- Sistem ayarlarını değiştirme -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" 
        tools:ignore="ProtectedPermissions"/> <!-- Uygulama kullanım istatistikleri -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> <!-- Diğer uygulamaların üzerinde gösterim -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" 
        tools:ignore="ProtectedPermissions"/> <!-- Erişilebilirlik servisi - güçlü sistem kontrolü -->

    <!-- Eski API'ler için depolama izni -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- UYGULAMA AÇMA BECERİSİ İÇİN - Tüm uygulamaları sorgulama -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- TELEFON BECERİSİ İÇİN - Kişileri okuma ve arama -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- UYGULAMA TANIMI -->
    <application
        android:name=".App"
        android:allowBackup="false"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <!-- ANA EKRAN - Maku'nun ana arayüzü -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop"
            android:showOnLockScreen="true">

            <!-- Uygulama başlatıcı -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Asistan olarak ayarlama -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="com.android.systemui.action_assist_icon"
                android:resource="@mipmap/ic_launcher" />

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- HATA EKRANI -->
        <activity
            android:name=".error.ErrorActivity"
            android:exported="false" />

        <!-- SESLİ TANIMA AÇILIR PENCERESİ -->
        <activity
            android:name=".io.input.stt_popup.SttPopupActivity"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/SttPopupTheme"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
            </intent-filter>
        </activity>

        <!-- SESLİ TANIMA SERVİSİ -->
        <service
            android:name=".io.input.stt_service.SttService"
            android:description="@string/stt_service_label"
            android:directBootAware="true"
            android:exported="true"
            android:foregroundServiceType="microphone"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/stt_service_label"
            android:permission="android.permission.RECORD_AUDIO">
            <intent-filter>
                <action android:name="android.speech.RecognitionService"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/stt_service_metadata" />
        </service>

        <!-- BİLDİRİM DİNLEME SERVİSİ -->
        <service
            android:name=".skills.notify.NotifyHandler"
            android:description="@string/skill_notify_handler_label"
            android:label="@string/skill_notify_handler_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- TELEFON AÇILINCA OTOMATİK BAŞLATMA -->
        <receiver
            android:name=".io.wake.BootBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- UYANMA KELİMESİ SERVİSİ - "Maku" dediğinde uyanır -->
        <service
            android:name=".io.wake.WakeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/wake_service_label" />

        <!-- ERİŞİLEBİLİRLİK SERVİSİ - Sistem kontrolü için -->
        <service
            android:name=".services.MakuAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>